<%= form_with(model: toki, local: true) do |form| %>
  <% if toki.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(toki.errors.count, "error") %> prohibited this toki from being saved:</h2>

      <ul>
        <% toki.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :clock_in %>    
    <%= form.datetime_select :clock_in %>
  </div>

  <div class="field">
    <%= form.label :clock_out %>
    <%= form.datetime_select :clock_out %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
