<%= form_with(model: lov_status, local: true) do |form| %>
  <% if lov_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lov_status.errors.count, "error") %> prohibited this lov_status from being saved:</h2>

      <ul>
        <% lov_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
